{
  "name": "Booking",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Customer full name"
    },
    "phone": {
      "type": "string",
      "description": "Customer phone number"
    },
    "email": {
      "type": "string",
      "description": "Customer email"
    },
    "vehicle": {
      "type": "string",
      "description": "Vehicle make and model"
    },
    "year": {
      "type": "string",
      "description": "Vehicle year"
    },
    "service": {
      "type": "string",
      "enum": [
        "interior",
        "exterior",
        "ceramic",
        "full",
        "maintenance",
        "unsure"
      ],
      "description": "Service type requested"
    },
    "add_ons": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of selected add-on names for this booking"
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Preferred appointment date"
    },
    "time": {
      "type": "string",
      "description": "Preferred appointment time"
    },
    "notes": {
      "type": "string",
      "description": "Additional notes from customer"
    },
    "status": {
      "type": "string",
      "enum": [
        "new",
        "confirmed",
        "completed",
        "cancelled"
      ],
      "default": "new",
      "description": "Booking status"
    },
    "total_price": {
      "type": "number",
      "description": "Final price charged for this booking in dollars"
    },
    "calendarEventId": {
      "type": "string",
      "description": "Google Calendar event ID for this booking"
    },
    "calendarBlockEventId": {
      "type": "string",
      "description": "Google Calendar block event ID (all-day booked marker) for this booking"
    },
    "membership_flagged": {
      "type": "boolean",
      "default": false,
      "description": "True if this maintenance detail booking was booked outside the 30-day eligibility window and needs admin review"
    }
  },
  "required": [
    "name",
    "phone",
    "vehicle",
    "service",
    "date",
    "time"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "data.email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.status": "confirmed"
        },
        {
          "data.status": "completed"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}